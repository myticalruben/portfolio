import { CustomCursor } from './custom-cursor';

describe('CustomCursor', () => {
  it('should create an instance', () => {
    const directive = new CustomCursor();
    expect(directive).toBeTruthy();
  });
});
